# AluMaster Email Configuration
# Copy this file to .env and update with your actual credentials

# cPanel SMTP Configuration (Recommended for hosting)
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=587
SMTP_USERNAME=alumaster75@yourdomain.com
SMTP_PASSWORD=your-email-password
SMTP_ENCRYPTION=tls

# Email Settings
FROM_EMAIL=alumaster75@yourdomain.com
FROM_NAME="AluMaster Aluminum System"
REPLY_TO=alumaster75@yourdomain.com
ADMIN_EMAIL=alumaster75@yourdomain.com

# cPanel Email Setup Instructions:
# 1. Login to your cPanel
# 2. Go to "Email Accounts" 
# 3. Create email: alumaster75@yourdomain.com
# 4. Use these common cPanel SMTP settings:
#    - SMTP Host: mail.yourdomain.com (replace with your domain)
#    - Port: 587 (TLS) or 465 (SSL) or 25 (non-encrypted)
#    - Username: full email address
#    - Password: the email account password

# Alternative cPanel SMTP Hosts (try if mail.yourdomain.com doesn't work):
# - smtp.yourdomain.com
# - yourdomain.com
# - server.yourdomain.com (check cPanel for exact server name)

# Other Email Providers (if not using cPanel):
# Gmail: smtp.gmail.com, port 587, TLS (requires App Password)
# Outlook: smtp-mail.outlook.com, port 587, TLS
# Yahoo: smtp.mail.yahoo.com, port 587, TLS